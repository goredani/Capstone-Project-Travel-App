import { formHandler } from '../src/client/js/formHandler'

describe("Testing how we handle the form submission", () => {
    test("Testing the form handler function", () => {
        expect(formHandler).toBeDefined();
    });
  });